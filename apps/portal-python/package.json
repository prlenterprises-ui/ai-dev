{
  "name": "portal-python",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "start": "python main.py",
    "build": "docker build -t portal-python:latest ."
  }
}
